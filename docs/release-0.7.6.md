# TableScan Local 0.7.6

Generated template, field, column, and rule names use English independently of the interface language. Template creation uses the source filename plus “— template”; duplication preserves the original name and appends “— copy”. Blank template/rule names fall back to “New template” / “Values”. Existing saved names and source filenames are preserved.

Validation: 29 targeted UI, localization, rule-editor, and storage tests passed, including name generation with English, Russian, and Czech interface settings.
